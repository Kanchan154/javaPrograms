import { element } from "prop-types";
import Movie from "./movies";
function App(){
    return(
        <div>className="App"
            <header\>
                <div className="main";
}
            Movie.map(element)=>{
                <Movie
                title={element.title}
                year={element.year}
                img={element.poster}
                />)
                }
                </div>
        </div>
    
}